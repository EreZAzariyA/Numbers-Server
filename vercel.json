{
  "version": 2,
  "builds": [
    {
      "src": "dist/**/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/app.js"
    },
    {
      "src": "/transactions/:user_id",
      "dest": "/api/transactions/:user_id"
    }
  ]
}
